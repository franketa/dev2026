const fs=require("fs"),p=require("path");const B="C:/Users/marti/OneDrive/Desktop/dev2026/clients/gran-palace";const d=JSON.parse(fs.readFileSync(p.join(B,"_data.json"),"utf8"));for(let[f,c]of Object.entries(d)){let fp=p.join(B,f);fs.mkdirSync(p.dirname(fp),{recursive:true});fs.writeFileSync(fp,Buffer.from(c,"base64").toString("utf8"));console.log("Written",f,Buffer.from(c,"base64").length,"bytes");}